# 已知局限性 / 未处理问题记录

这份文档诚实记录当前RAG系统里已知的检索缺陷和数据处理层面的取舍。原则和 `CLAUDE.md` 一致:能修的已经修了,修了之后的实际效果(包括没达到预期的情况)如实记录;暂不修的说清楚为什么现阶段不投入,以及权衡依据是什么。

## 0. 页码与来源定位(已解决——全量改用MinerU重新解析)

### 背景

之前文本知识类chunk的 `position` 字段只有字符偏移量估算值(`char_start`/`char_end` + `estimated: true`),不是真实页码。调查确认:Docling的`DoclingDocument`对象本身每个item都带精确`prov.page_no`,`export_to_markdown()`也支持`page_break_placeholder`参数,但当初调用时没传,页码信息在导出那一步被丢弃;LlamaParse的API返回本身按页分段(`result.markdown.pages`),但存盘前直接拼接成一个字符串,且事后用`client.parsing.get(job_id)`实测确认服务端解析结果已过期(只剩job状态,markdown字段是`None`)——两条路都不能免费在原有数据基础上补回页码。

### 解决方案

全量445个PDF(6个之前已标记`pending`暂缓的除外,439个)改用MinerU重新解析,替换原来Docling/LlamaParse混合的产线:

- 硬件: RTX 5070(Blackwell架构,sm_120)之前装的是CPU-only build的torch,换成官方cu128构建后`MINERU_DEVICE_MODE=cuda`可用。实测GPU相比CPU在公式密集/长文档上加速明显(25页/250处公式的样本: CPU 292秒 → GPU 40秒,约7倍),短文档提升有限(模型加载是固定开销)。
- 调用方式: 每批多个文件一次性喂给mineru CLI而不是逐文件单独起进程,避免模型加载的固定开销被文件数放大(实测5个文件: 单独跑156秒,一次性跑51秒)。
- 全量439个文件实际耗时约58分钟,439/439全部成功,零解析失败。
- 页码来源: MinerU的`content_list.json`给每个内容块(文字/公式/代码)标注了精确的`page_idx`,用块内容前40字符在导出的markdown里定位锚点,再把chunk的字符偏移量映射到最近的锚点页码——不是字符偏移估算,是从模型输出直接继承的页码。
- 实测覆盖率: 全量25789个chunk里,只有15个(<0.06%)因为源文件本身没有可提取文字(纯ER图/示意图PDF,如`design.pdf`、`geography.pdf`)拿不到锚点,退化为无页码;其余全部有真实`page_start`/`page_end`。

### 已知的连带影响(如实记录)

- chunk总数从21206变成25789(+21.6%)——MinerU的OCR分块粒度跟Docling不同,同样的header+字符切分逻辑在不同的源markdown上会切出不同数量的chunk,这是切分对象变了的自然结果,不是bug。
- 之前诊断的14条中文query失败案例已经用新语料重新验证,结果和分析见下方**0.2节**。
- MinerU在少数文档上有表格丢失、公式误识别为看似合理但语义错误的LaTeX等已知风险,具体的检测机制和人工抽查结果见下方**0.1节**。

## 0.1 公式可信度风险(未解决,已做到"有标记"——如实记录检测局限)

### 背景

"MinerU Trial Proofs"评估发现的核心风险是"格式规整但语义错误的LaTeX"——公式转写失败时,Docling会诚实地留一个`formula-not-decoded`占位符,而MinerU会给出一段看起来完全合理、实际内容是编造的LaTeX。这个风险原样存在于全量439个文件、25789个chunk里,评估阶段只在5个样本上验证过,没有量化过实际比例。这次做了两件事:能自动检测的部分接上检测,不能自动检测的部分老实说清楚检测不到。

### 置信度信号:哪些能自动标记,哪些不能

实测检查了MinerU的`middle.json`(15个随机抽样文档,439个文档里的一部分):

| 公式类型 | 对应content_list.json类型 | 是否有置信度score | 实测覆盖率 |
|---|---|---|---|
| 行内公式(inline_equation) | 内嵌在text块里,不单独成块 | **有**,来自布局检测模型 | 15个文档624个inline span,624个都有score(100%) |
| 独立成行/多行公式(interline_equation) | `type: "equation"`的独立block | **没有**,结构性缺失 | 15个文档252个interline span,0个有score(0%) |

这不是抽样误差,是MinerU pipeline backend本身的架构特征:行内公式的置信度信号来自布局检测阶段并一路带到最终输出,独立公式块经过裁剪图像→公式识别(MFR模型)的单独流程,识别结果没有把置信度带出来。

基于这个实测结果,做了两个标记(`ingest/chunk_text_mineru.py`):

- **`formula_low_confidence`**:chunk所在页面有行内公式的置信度低于0.6(全库29473个行内公式score分布的约第12百分位,min=0.45、median=0.87,没有天然的双峰分界点,选一个偏保守的整数分位点,不是精调出来的)。
- **`formula_unverifiable`**:chunk所在页面存在独立公式块。这不是"低置信度"标记,是如实标注"这里有一段完全没有办法自动判断对错的公式内容"——因为**人工抽查发现,真正的"看起来对但是错的"风险几乎全部出在这类公式上**(见下方抽查结果),而这类公式恰恰是没有任何置信度信号能覆盖的。

两个标记都是**页面粒度**,不是精确到"这个chunk自己的文字里有没有公式"——chunk如果和某个公式共享同一页,即使chunk本身的文字不含公式,也会被标记。这是当前锚点定位精度(块级,不是字符级)决定的,标记会偏保守(宁可多标,不漏标),不是bug。全库25789个chunk里,13600个(52.7%)被标`formula_unverifiable`,9522个(36.9%)被标`formula_low_confidence`——比例高是因为这批课程材料(概率/优化/机器学习)本身75.4%的文档(331/439)至少包含一处公式,不是标记逻辑过度触发。

### 人工抽查结果(具体数字,不是模糊表述)

**样本1:6份清晰印刷体文档(教材/课件PPT/习题解答),18处公式,覆盖good/degraded/llamaparse_done三种旧quality标签** —— 逐一打开原始PDF页面用肉眼核对,**18/18(100%)转写正确**,包括跨页公式、多行推导、matrix/argmax等复杂结构。这批文档旧标签虽然有的是"degraded"(旧Docling判定质量不足),但源本身是清晰的电子版/扫描印刷体,不是手写。

**样本2:1份真正手写笔记文档(`IEOR E4106 Stochastic Model\1 Review of Conditional Probability.pdf`,就是评估阶段发现风险的那份原始样本)** —— 人工核对了11处公式(逐页对照原始PDF图像):

| 结果 | 数量 | 占比 |
|---|---|---|
| 转写正确(含允许的记号简化,比如箭头下标转成普通下标) | 4 | 36% |
| 转写错误 | 7 | 64% |
| 其中:格式规整但语义完全错误(**危险类型**,读者不会察觉) | 4 | 36% |
| 其中:明显乱码/符号错乱(读者容易察觉是错的) | 2 | 18% |
| 其中:结构对但个别细节错(比如全概率公式凭空多了个`/2`) | 1 | 9% |

**结论**:公式转写风险高度集中在"源材料本身难以辨认"(手写、模糊扫描)这一类文档上,在清晰印刷体材料上基本不出现。这两个样本的对比本身就是最直接的证据,不需要额外假设。危险类型(格式对但语义错)在手写样本里占到36%,这是当前系统完全没有能力自动拦截的风险等级——`formula_unverifiable`标记只能告诉下游"这里有公式、无法验证",不能告诉下游"这处公式大概率是错的"。

### 决策:标记但不解决

不做进一步处理(比如对手写类文档整体做二次人工校对,或者引入另一个模型做交叉验证)——本轮任务范围是"让风险可见",不是"消除风险"。如果后续要认真使用手写笔记类内容(尤其是需要精确公式的场景,比如生成作业解答),这里的`formula_unverifiable`标记是必须先处理掉的已知缺口,而不是可以忽略的噪声。

## 0.2 中文检索14个已知案例:重新诊断结果

### 方法

`evaluate/_diag_text_miss_data.json`里的14个案例是按旧的`txt_XXXXX` chunk_id标注目标内容的,全量换用MinerU重新分块后chunk_id和边界都变了,不能直接复用。重新定位方法:用旧chunk_id查旧`text_chunks.json`拿到源文件路径,在新`text_chunks_mineru.json`里筛出同一文件的所有候选chunk,用旧chunk文字里的实词(过滤markdown符号后长度≥3的英文单词/数字)跟每个候选chunk做重合度打分,取重合度最高的作为新目标。14个案例里12个重合度在70%以上(比如`txt_02341`→`txtm_02981`重合32/34词),可信;2个重合度偏低(`txt_10244`的41%、`txt_15110`的33%),标注为"重定位存疑"——`txt_15110`那个案例(问"第二列的数值范围"针对的是一份被OCR成表格的数据图)在MinerU输出里没找到任何一个chunk包含原表格数值,推测MinerU把这部分识别成了图片而不是表格文字,原问题针对的内容可能在新pipeline下根本不再以可检索文字形式存在——这本身也是一个值得记录的行为差异,不是重定位方法失败。

对重定位后的12个可信案例,取全库精确排名(向量29032条全排、BM25全排,不是只看top-10或top-50)对比新旧排名:

| chunk_id(旧→新) | 问题 | 向量排名(旧→新) | BM25排名(旧→新) |
|---|---|---|---|
| txt_16272→txtm_17555 | Gurobi非凸参数设置 | 54→126 ↓ | 7→9 ↓ |
| txt_05129→txtm_06845 | B+树索引类型 | 6992→14288 ↓ | 979→1354 ↓ |
| txt_05591→txtm_07507 | SQL连接查询例子 | 1469→1448 ↑(微) | 5857→7792 ↓ |
| txt_05618→txtm_07547 | SQL统计教师课程数 | 2773→2115 ↑ | 360→323 ↑ |
| txt_17615→txtm_19873 | 乘客到达公交站过程 | 4331→17283 ↓ | 17616→19874 ↓ |
| txt_00382→txtm_00345 | WordPiece子词处理 | 41→37 ↑(微) | 7→8 ↓(微) |
| txt_15857→txtm_17125 | 整数规划vs线性规划 | 2345→3243 ↓ | 15858→17126 ↓ |
| txt_06560→txtm_08533 | 可分情况找超平面 | 18333→20563 ↓ | 6561→8534 ↓ |
| txt_02341→txtm_02981 | DNC读取头内容查找 | 6→84 ↓ | 6→14 ↓ |
| txt_20002→txtm_23850 | 生成离散随机变量X | 1678→3824 ↓ | 1116→1740 ↓ |
| txt_17596→txtm_19823 | 第一辆公交车上车人数 | 37→4836 ↓ | 5844→964 ↑(大) |
| txt_10244→txtm_11947 | 多输入通道输出计算(重定位存疑,41%重合) | 10591→18353 ↓ | 10245→11948 ↓ |

12个案例里,向量排名变好的2个(txt_05618、txt_00382,均为微幅)、基本不变的1个(txt_05591)、变差的9个;BM25排名变好的2个(txt_05618、txt_17596)、变差的10个。**没有一个案例进入新的top-5**,和旧pipeline下的结果一致——这14个案例本来就是"已知失败"案例,不是这次改动的回归测试基准,不能反过来当"MinerU让检索变差了"的证据,但确实观察到了一个系统性趋势需要如实记录。

### 系统性趋势:chunk池扩大带来的稀释

多数案例排名变差,且比例明显偏向"变差"(12个案例里9-10个两个维度都在变差),这和之前在`LIMITATIONS.md`第3节记录的"C类检索池稀释"是同一种机制:chunk总数从21206增加到25789(+21.6%),意味着BM25的IDF统计基数变了、向量检索的近邻竞争者变多了——同样内容,candidate pool变大,原本排名就不高的目标被进一步稀释,这是纯粹的"分母变大"效应,不是MinerU转写质量比Docling差导致的(需要能观察到具体是"目标内容本身可读性变差"才能这么归因,而抽查发现新chunk的文字内容跟旧版本相比没有明显退化)。

### 结论:A/B/C三类根因,一个都没有被这次改动解决

这次改动是**换解析工具**,不是**换检索策略/换embedding模型**,而A类(中文query在英文embedding模型下表现差)、B类(关键词字面缺失)这两类根因都跟"用什么工具解析PDF"无关——本来就不该指望这次改动修好它们,实测结果也确认了这一点(没有一个案例的失败模式发生变化)。**如实记录:这次改动对已知的检索质量问题没有正向作用,反而因为chunk池扩大带来了轻微的负面稀释效应**。要真正解决A/B/C类问题,仍然需要之前记录过的方案(多语言embedding模型、query rewriting/expansion),这次的"换MinerU"工作跟那条路线是正交的,不能相互替代。

## 1. 文本知识类的文件名索引修复(已实施,效果有限——如实记录;该实验基于的旧Docling产线已被上方第0节替换,这里保留作历史记录)

### 背景

在35条RAGAS测试集的诊断中发现,文本知识类的检索命中率只有6.7%(15条里1条命中目标chunk)。对14个未命中案例做了逐条根因排查(取全库精确余弦相似度 + BM25精确排名,不只看top-10),分成三类:

- **A类 语言不匹配**(7例):中文query在当前BM25分词器下产出空token列表,向量相似度也很低(embedding模型是纯英文的all-MiniLM-L6-v2)
- **B类 词汇鸿沟**(3例:`txt_05591`、`txt_05618`、`txt_20002`):query里的关键词在目标chunk原文里字面上没出现
- **C类 检索池被稀释**(4例):目标chunk分数/排名其实不差,只是没挤进最终top-5

### 修复方案与实施

对B类,复用了之前修 Caregivers SQL 案例(`ingest/parse_code.py::parse_sql()`)时用的方案——把文件名拼进可索引文本。具体改动:`ingest/chunk_text.py::chunk_document()` 里,每个文本chunk存入索引的正文,现在会在最前面加一行不含路径的源文件名(比如 `Section 2 Worksheet.pdf`),质量检测(`detect_chunk_quality_flags`)和position定位仍然基于不含文件名的原始内容,避免失真。改完后重新跑了全流程:`run_chunk_text_full.py` → `merge_chunks.py` → `build_bm25_index.py` → `build_simple_index.py`,chunk总数 24311(文本21206 + 代码3105),索引全部重建。

### 实测结果(如实记录:未达到预期)

用B类的3个案例重新测了BM25排名和向量排名:

| chunk_id | 查询关键词 | BM25排名(修复前→后) | 向量排名(修复前→后) |
|---|---|---|---|
| txt_05591 | sql | 5857 → 5858 | 1469 → 1167 |
| txt_05618 | sql | 360 → 456 | 2773 → 2993 |
| txt_20002 | x | 1116 → 1125 | 1678 → 1284 |

**结论:三个案例里,BM25排名没有一个变好(两个还略微变差),向量排名两升一降,且没有一个进入最终top-5。** 根因排查后发现:这3个chunk对应的源文件名本身并不包含缺失的关键词——`txt_05591`/`txt_05618` 来自 `Section 2 Worksheet.pdf`/`Worksheet Solutions.docx.pdf`,`txt_20002` 来自 `IEOR4404_HW4.pdf`,文件名里都没有 "sql" 或和目标query强相关的词。这和最初参照的 Caregivers SQL 案例不同——那次是文件本身就叫类似"...SQL..."这样的名字,文件名天然带着缺失的关键词;这3个文本案例只是主题相关但命名方式不同,文件名修复补不上这个缺口。

另外,给全部21206个文本chunk统一加前缀,会小幅改变BM25的全局统计量(文档长度分布、IDF),这正是之前修分词器时观察到的K-means副作用同一种机制——对个别case可能有轻微负面影响,这里两个case排名变差就是这个机制的体现。

**是否保留这次改动**:保留。理由是:(1) 这个方案本身是正确的通用改进——文件名经常携带课程、作业编号等语义信息,能帮助本次未覆盖到的其他查询;(2) 对全局的负面影响量级很小,属于已知的、可解释的权衡,和分词器修复时的处理原则一致;(3) 但对这3个具体audit case没有解决问题,说明"词汇鸿沟"这个问题还没有根本性方案,如果要真正修复需要query expansion或更换多语言/领域适配的embedding模型,现阶段不在本轮投入范围内。

## 2. A类:中文query的检索局限(未处理,记录评估结论)

当前embedding模型(all-MiniLM-L6-v2)针对英文优化,中文query的向量检索和BM25分词均存在已知局限——这是基于语料库以英文为主(courses材料中文占比<1%)这一现状做出的性价比选择。如果后续中文查询需求增加,可考虑更换为multilingual-e5-large等多语言模型,但需要重新评估整体检索效果(担心引入新的副作用,类似之前BM25分词器修复引发的K-means案例连带影响)。

## 3. C类:检索池稀释(未处理,记录评估结论)

部分查询存在检索池被稀释的现象(真实相关内容排名接近但未进入最终top-5)。已知的解法包括query rewriting或扩大候选池,经评估后判断当前阶段暂不投入,原因是候选池扩大可能引入新噪声,且没有独立于语义/关键词检索之外的根本性解法(和之前K-means案例分析结论一致)。

## 4. 数据覆盖范围:6个文件主动排除/搁置

445个文本知识类PDF中,**439个**进入知识库构建流程(chunk库、索引、检索全部覆盖),**6个**因预算约束主动排除/搁置,完整名单和排除原因记录在 `ingest/_test_output/quality_manifest.json`(`quality: "pending"` + `pending_reason` 字段):

- **5个:`pending_reason: "deferred_low_priority"`** —公开教材/期刊论文,内容网络可查、非课程原创,优先级判断后本轮暂不处理:
  - `ECON 484\Polya_HowToSolveIt.pdf`
  - `ECON 484\casi.pdf`
  - `ECON 484\ISLRv2_website.pdf`
  - `ECON 484\First Week Reading\1177704711.pdf`(JSTOR期刊文章)
  - `IEOR E4106 Stochastic Model\Introduction to Probability Models, 11th Edition" by Ross.pdf`
- **1个:`pending_reason: "deferred_budget_timeout"`** —课程原创材料,Docling处理超时,需要额外LlamaParse预算才能重新解析,本轮预算内暂缓:
  - `IEOR E4004 Optimization Models & Methods\L6\IEOR4004-Class6.pdf`

> 注:manifest里没有单独的 `"excluded"` 状态——按之前的决定,所有排除/搁置的文件统一标记为 `quality: "pending"`,用 `pending_reason` 区分"低优先级搁置"和"预算/超时搁置"两种原因,已核对该字段在6条记录里都完整一致(`quality`/`pending_reason`/`note`/`course`/`pages` 均齐全),无需补齐。
